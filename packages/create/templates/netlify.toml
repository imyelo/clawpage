[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  BUN_VERSION = "latest"

# Netlify-specific redirects (optional)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
