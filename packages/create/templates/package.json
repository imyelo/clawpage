{
  "name": "my-chats-project",
  "private": true,
  "scripts": {
    "dev": "clawpage-web dev",
    "build": "rimraf ./dist && clawpage-web build"
  },
  "dependencies": {
    "clawpage-web": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "rimraf": "^6.0.1"
  }
}
